# Ontology chat agent. Installed into the ARM64 container image (Dockerfile, built by
# apps/ai/agents/build_agent_container.sh), not a zip: claude-agent-sdk shells out to
# a `claude` CLI the image has to carry, which puts the bundle over the zip ceiling.
#
# No numpy or scipy, unlike the build agent. Retrieval does no linear algebra of its
# own: S3 Vectors ranks the windows, and the neighbour cut is a sum over a few dozen
# edges in plain Python.
claude-agent-sdk
bedrock-agentcore
boto3
