# Orchestrator (A2A client side). Built into the --only-binary aarch64 zip by
# ai/agents/build_agent.sh, so every (transitive) dep must ship a cp312 aarch64 wheel.
#
# strands-agents-tools provides A2AClientToolProvider (the out-of-the-box A2A fleet
# client). Pin >=0.2.14: that's the first release with BOTH the #263 fix (it no longer
# re-derives the AgentCore invocation URL — it honours known_agent_urls) AND the
# httpx_client_args parameter we use to forward the Cognito bearer token + session-id
# to subagents. a2a-sdk is pinned to the SAME range as the subagent
# (ai/agents/number_specialist/requirements.txt) so client and server never drift.
strands-agents
strands-agents-tools>=0.2.14
bedrock-agentcore
a2a-sdk>=0.3,<0.4
boto3
# AgentCore observability: provides the `opentelemetry-instrument` entrypoint the
# runtime is launched with (see infra ChatAgent entry_point) plus auto-instrumentation.
# Must ship a cp312 aarch64 wheel so it lands in the --only-binary agent zip.
aws-opentelemetry-distro>=0.10,<1.0
